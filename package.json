{
  "name": "newaitools.online",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npx tsx generate-sitemap.ts && npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
